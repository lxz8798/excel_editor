export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  // 默认的路径
  // BASE_HOME = '/dashboard',
  BASE_HOME = '/account/center',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
